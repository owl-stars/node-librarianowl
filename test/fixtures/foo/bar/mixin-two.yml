description: Mixin two Lorem ipsum
mixin:
  parameters:
    a: "a lorem ipsum"
    b: "b lorem ipsum"
  less: |
    .foo-bar-mixin-two(@a, @b) {
      display: block;
    }
  sass: |
    @mixin foo-bar-mixin-two($a, $b)
      display: block
  scss: |
    @mixin foo-bar-mixin-two($a, $b) {
      display: block;
    }
  styl: |
    foo-bar-mixin-two(a, b)
      display: block;
examples:
  sass: |
    .example-mixin-two
      +foo-bar-mixin-two(-10px, -10px)
  scss: |
    .example-mixin-two {
      @include foo-bar-mixin-two(-10px, -10px);
    }
  less: |
    .example-mixin-two {
      .foo-bar-mixin-two(-10px, -10px);
    }
  stylus: |
    .example-mixin-two
      foo-bar-mixin-two(-10px, -10px)
  html: |
    <div class=".example-mixin-two"></div>
