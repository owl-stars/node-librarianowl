description: Mixin one Lorem ipsum
mixin:
  parameters:
    a: "a lorem ipsum"
    b: "b lorem ipsum"
  less: |
    .foo-bar-mixin-one(@a, @b) {
      display: block;
    }
  sass: |
    @mixin foo-bar-mixin-one($a, $b)
      display: block
  scss: |
    @mixin foo-bar-mixin-one($a, $b) {
      display: block;
    }
  styl: |
    foo-bar-mixin-one(a, b)
      display: block;
examples:
  sass: |
    .example-mixin-one
      +foo-bar-mixin-one(-10px, -10px)
  scss: |
    .example-mixin-one {
      @include foo-bar-mixin-one(-10px, -10px);
    }
  less: |
    .example-mixin-one {
      .foo-bar-mixin-one(-10px, -10px);
    }
  styl: |
    .example-mixin-one
      foo-bar-mixin-one(-10px, -10px)
  html: |
    <div class=".example-mixin-one"></div>
